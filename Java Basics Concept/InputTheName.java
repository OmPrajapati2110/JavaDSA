import java.util.*;
public class InputTheName {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // String input = sc.next(); //This will only return the characters till space or only one word
        // System.out.println(input);

        /* String name = sc.nextLine(); //nextLine will print the entire statement
        System.out.println(name); */

        /* int num = sc.nextInt(); //This will print all the integers and anything other than that will throw an error
        System.out.println(num); */

        /* float price = sc.nextFloat(); //This will return all the floating point numbers
        System.out.println(price); */

        /* int bytes = sc.nextByte(); //This will only return integers not even float
        System.out.println(bytes); */

        /* double result = sc.nextDouble(); //THis will return only integers but in floating format
        System.out.println(result); */

        
    }
}
